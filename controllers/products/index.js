const allProducts = require('./allProducts');
const queryProducts = require('./queryProducts');
const getDiet = require('../products/getDiet');

module.exports = { allProducts, queryProducts, getDiet };
