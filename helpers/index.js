const RequestError = require('./RequstError');
const createToken = require('./createToken');
const calculateDiet = require('./calculateDiet');

module.exports = {
  RequestError,
  createToken,
  calculateDiet,
};
